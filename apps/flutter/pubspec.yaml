name: hash_killer_frontend
description: Flutter frontend for hash-killer.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.5

dependencies:
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  forui: ^0.21.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
